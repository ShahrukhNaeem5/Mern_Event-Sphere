[phases.install]
commands = ["cd backend && npm install", "cd frontend && npm install"]

[phases.build]
commands = ["cd frontend && npm run build", "mv -r frontend/build backend/"]

[phases.start]
commands = ["cd backend && npm start"]
